/**
 * Point d’entrée types pour le schéma `learning_path` (hors `Database` public généré).
 * Réexport depuis la logique métier — source unique avec `getLearningPathClient` / `completeLesson`.
 */
export type { LearningPathDatabase } from '@/lib/learningPath';
